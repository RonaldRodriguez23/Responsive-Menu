const letraR = document.querySelector('.letra-r');

letraR.addEventListener('mouseover', () => {
letraR.classList.add('letra-r-hover');
});

letraR.addEventListener('mouseout', () => {
letraR.classList.remove('letra-r-hover');
});

